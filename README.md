# i2c

Convert images to code using [VinciCoder-8B-SFT](https://huggingface.co/DocTron-Hub/VinciCoder-8B-SFT).

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Usage

```bash
python -m i2c.test
```
